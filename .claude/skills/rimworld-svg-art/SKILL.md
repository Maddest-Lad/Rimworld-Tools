---
name: rimworld-svg-art
description: Make RimWorld-style game art as SVG (hand-written, or generated by a small Python script for sets) and turn it into game-ready PNG textures (1.6, Windows/Steam) - weapons, items and resource stacks, furniture and buildings (Graphic_Multi + CutoutComplex masks), storage with fill levels, linked fences/walls, doors, floors/terrain, apparel and pawn parts, animals, plants, gene/ability/command icons. Grounded in the game's own sprites and code - ready-made vanilla sprite sheets and per-category stats (assets/), sheets from any installed mod (build_mod_refs.py), texture-replacement detection (vanilla_tex.py --overrides), analyze.py to measure and compare, render.py to rasterise/lint/preview one sprite at in-game zoom with stuff tints and masks, compose.py to preview multi-part scenes (runs, fills, fence pens, doors) as the engine lays them out. Use when the user wants textures, sprites, icons or other art for a RimWorld mod, wants art to match vanilla or another mod's style, or asks how RimWorld art is made.
---

# RimWorld art as SVG

You write SVG as text (shapes, not pixels), render it to PNG and judge it the way the game shows
it. Scripts carry PEP 723 headers — `uv run` installs UnityPy/resvg-py/Pillow/numpy on first
use. Run from the repo root; `A=.claude/skills/rimworld-svg-art/scripts`.

## Ground rules

- **Pin the style reference before drawing** (step 1). "Vanilla" is not one thing: the user may
  run texture-replacement mods, or want another mod's look. Everything later is measured against
  that reference, and the reference beats the generic rules in `reference/style.md`.
- **Decisions live with the mod, not in this skill.** Chosen references, palettes, material
  recipes and what this user rejected go in `workspace/<Mod>/Art/STYLE.md`. Read it first;
  update it in the same turn the user corrects something. The skill only holds rules that are
  true of RimWorld art in general.
- **Show one piece before building a set.** Get the user's OK on one sprite (one rotation, one
  fill level) in a preview, then generate the variants. Most rework comes from set-wide choices
  (depth cue, pattern, proportions) the user would have rejected on the first piece.
- **Preview at game scale, composed.** The native PNG lies: at the starting zoom a cell is ~30
  px. Single sprites → `render.py --preview`; anything drawn from several textures (tiled runs,
  body + fill, linked fences, doors) → `compose.py`. `Read` the sheet every iteration.
- **Reference art is study-only.** Vanilla and mod textures (`assets/`, `.cache/`) are other
  people's art: measure, compare, underlay with `id="guide…"` (stripped at render) — never
  copy, trace or ship. All of it is gitignored.
- **Sources live with the mod:** SVG (or its generator script) in `workspace/<Mod>/Art/`, PNGs in
  `workspace/<Mod>/Textures/<Mod>/…` (`/rimworld-mod-dev`). Never write into `links/`.

## Workflow

1. **Reference and profile.**
   - Ask which look to match if the user hasn't said: vanilla, a named mod, or "what I see in my
     game". For the last, `uv run $A/vanilla_tex.py --overrides '<vanilla path regex>'` lists
     installed mods that replace those textures (e.g. upscale packs) — that is their "vanilla".
   - Vanilla: `assets/INDEX.md` (sheets for 31 categories) + `assets/stats.md` (numbers). If
     missing: `make art-refs` (~30 s). A mod: `uv run $A/build_mod_refs.py --name … --mod <pfid>
     --regex …` → `assets/mods/INDEX.md` (+ stats).
   - Copy `templates/STYLE.md` to `workspace/<Mod>/Art/STYLE.md` (if the mod has none) and fill
     it from the sheets and `analyze.py --summary`: references, px per cell, outline, one recipe
     per recurring material, depth cue, and an (initially empty) Rejected list. Confirm it with
     the user. Every later piece follows it.
2. **Spec from the def.** `graphicClass`, `drawSize`, `shaderType`, stuff-tinted or own colour,
   parts that keep a fixed colour (mask), fill layers, linked/door/terrain behaviour. Engine
   rules: `reference/geometry.md`. Canvas and outline: the profile, else `reference/style.md`.
3. **One piece.** Silhouette on a cell-unit viewBox → layers (`reference/svg-craft.md`):
   outline underlay → flat fills (white/grey if tinted) → clipped shading, light from above →
   inner lines in a darker local colour. Render, lint, preview, compare
   (`analyze.py --against <reference> --ppc <yours> --ref-ppc <theirs>`). Show the user.
4. **The set.** Variants, rotations, fill levels, tiling pieces: write a generator script that
   emits the SVGs (`svg-craft.md` → Generated SVG) so shared profile numbers (post size, rim
   height) stay identical across pieces, and every recurring material comes from one shared
   module (`style.md` → Consistency across a set). Render all, `compose.py` the scenes.
5. **Review the set.** `sheet.py` over every texture at `--zoom native,30`: is each material
   drawn the same way everywhere, are different things distinguishable, are variants visibly
   different? Then check `style.md` → Common rejections and the mod's Rejected list.
6. **Wire up and test in game** (`/rimworld-mod-dev` → `reference/assets.md`): every rotation,
   every stuff, zoomed in and out, fill levels, linked neighbours.

## Tools

| Need | Command |
|---|---|
| Vanilla sheets / stats | read `assets/INDEX.md`, `assets/stats.md`; rebuild `make art-refs [ARGS=--force]` |
| A mod's sheets | `build_mod_refs.py --name mo_storage --mod 3219596926 --regex 'Things/Building/Storage/'` (`--list`) |
| Mods replacing vanilla textures | `vanilla_tex.py --overrides 'furniture/bed/|eggbox'` |
| Find / extract vanilla textures | `vanilla_tex.py --search REGEX` / `--extract REGEX [--limit N]`; `--stats things/building` |
| Measure, baseline | `analyze.py FILES…`, `analyze.py 'GLOB' --summary` |
| Compare to a reference | `analyze.py mine.png --against 'GLOB' [--ppc 128 --ref-ppc 64]` (per-cell outline/margin when densities differ) |
| SVG → PNG + lint | `render.py X.svg [--size 128 \| 256x128] [--out DIR] [--allow edge,outline]` |
| One-sprite preview | `render.py … --preview [FILE] --draw-size 2,1 --tint steel,wood --bg soil --ref REGEX [--ref-draw-size auto] [--tint-refs] [--screen 1080]` |
| Multi-texture scene | `compose.py scene.json` (layers, `linked` atlases, doors, vanilla neighbours; see its docstring) |
| Whole set side by side | `sheet.py 'Textures/<Mod>/**/*.png' --out Art/review/set.png --tex-ppc 128 --zoom native,30 [--tint wood --tinted REGEX]` |

Lint codes and intent: `[pow2]` (vanilla ships some non-power-of-two sizes), `[edge]`
(tileable/linked/seamless pieces touch edges on purpose), `[outline]` (fill overlays sit inside
another sprite's outline), `[faint]`, `[mask]`, `[multi]` (a note — the game substitutes missing
sides). Accept intended ones in the SVG: `<!-- svg-art: allow edge, outline -->`.
Tints: white, steel, wood, plasteel, gold, silver, uranium, jade, granite, marble, slate,
sandstone, limestone, cloth, leather, devilstrand, hyperweave, synthread, or `#rrggbb`.
Backgrounds: soil, gravel, concrete, sand, wood, dark.

**Shell pitfalls (Windows + Git Bash):** inline `python - <<EOF` mangles backslashes and `\n` —
write helper scripts to files. `/tmp` in bash is not visible to Windows Python; use workspace or
`%TEMP%` paths (`C:/Users/…/AppData/Local/Temp/…`) with forward slashes.

## Templates (`templates/`)

| File | Shows |
|---|---|
| `weapon_melee.svg` | 128 px stuff-tinted weapon: parts in defs, shared outline underlay, clipped shading |
| `resource_a/b/c.svg` | 64 px `Graphic_StackCount`: one piece reused with `<use>`, faceted own-colour shading |
| `crate_{north,east,south}.svg` + `…m.svg` | 1×1 `Graphic_Multi` in cell units, front face at the bottom in every rotation, masks |
| `plant.svg` | no black outline (darker own-hue edge), mirrored drooping leaves, `feTurbulence` grain |
| `icon_gene.svg` | 128 px UI icon: bold 5 px outline, 2–3 flat colours, ~10 % margin |
| `STYLE.md` | the per-mod style profile: references, scale, materials, depth, Rejected list |

## References

- `reference/style.md` — measured vanilla look, per-category spec, containers & fills, set consistency, common rejections
- `reference/geometry.md` — engine layout: drawSize swap, Multi fallbacks, linked atlas, doors, storage layers, terrain scale
- `reference/svg-craft.md` — SVG technique: outline underlay, clipped shading, grain, masks, generated SVG, tiling, seamless floors
- `reference/technical.md` — PNG loading (mipmaps, DXT, power of two, bleed), px per cell vs zoom, shaders
- `reference/sources.md` — where each fact came from; `assets/README.md` — the reference sheets
