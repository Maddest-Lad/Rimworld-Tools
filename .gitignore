# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/
dist/
build/
*.egg-info/

# Local settings may contain secrets. Only .env-template is tracked.
.env
.env.*
!.env-template

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Legacy SteamCMD installations remain local and are never staged.
bin/steamcmd/

# SteamCMD force_install_dir: holds the ACF and a junction INTO the real Mods folder.
# Git on Windows recurses through junctions, so this must never be staged.
bin/steam/

# Synced community databases
bin/dbs/

# Local
docs/
